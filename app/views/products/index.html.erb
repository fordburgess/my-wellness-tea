<div class="flex flex-col">
  <p class="mb-10">Products index page</p>
  <div class="flex mb-20">
    <% @products.each do |product| %>
      <p><%= product.title %></p>
    <% end %>
  </div>
  <%= link_to "Home", root_path, class: "text-blue-400" %>
</div>
